.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Rust build output for the XMP FFI crate. The committed artifact is
# artifacts/XMPToolkit.xcframework, regenerated by scripts/build-xmp.sh.
# Cargo.lock IS committed (under rust-xmp/) so the XCFramework rebuild is
# reproducible.
rust-xmp/target/
